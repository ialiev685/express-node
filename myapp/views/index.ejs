<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% function carView(item, i){ %>
    <tr>
        <td><%= i %></td>
        <td><%= item.name %></td>
        <td><%= item.price %></td>
    </tr>
    <% } %>

    <table border="1">
        <tr>
            <th>#</th>
            <th>model</th>
            <th>price</th>
        </tr>
        <% cars.map(carView) %>
    </table>
</body>
</html>